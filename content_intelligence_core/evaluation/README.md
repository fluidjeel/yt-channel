# Evaluation — Layer 1

**Purpose:** Confidence scoring, evidence scoring, validation before generation or publish.

**Planned:**
- Style match score (artifact vs niche + brand rules)
- Evidence links (claim → source video/comment/frame)
- Validation gates (min confidence to generate)

**Status:** **PLANNED** — no code yet.

Quality rules **definitions** live in `niches/*/quality_rules.yaml`; evaluation **engine** lives here.
